# 🚨 立即修复步骤

## 问题确认

从浏览器控制台看到，前端仍在访问：
```
http://8.148.177.179:5000/api/health ❌
```

应该是：
```
/api/backend/health ✅
```

## ✅ 配置检查清单

### 1. vercel.json 配置 ✅

文件位置：`frontend/vercel.json`

```json
{
  "rewrites": [
    {
      "source": "/api/backend/:path*",
      "destination": "http://8.148.177.179:5000/api/:path*"
    }
  ]
}
```

**配置正确！** ✅

### 2. 前端代码配置 ✅

文件位置：`frontend/src/services/api.js`

```javascript
const API_BASE_URL = import.meta.env.PROD 
  ? '/api/backend'  // 生产环境强制使用 Vercel 代理
  : (import.meta.env.VITE_API_BASE_URL || 'http://localhost:5000/api');
```

**配置正确！** ✅

## 🔧 需要执行的操作

### 步骤 1: 检查并删除 Vercel 环境变量

1. 登录 [Vercel 控制台](https://vercel.com)
2. 进入项目 `ship-steel`
3. **Settings** → **Environment Variables**
4. 查找 `VITE_API_BASE_URL`
5. **如果存在且值为 `http://8.148.177.179:5000/api`，请删除它**
6. 点击 **"Save"**

### 步骤 2: 提交代码到 Git

在项目目录执行：

```bash
cd frontend

# 查看修改的文件
git status

# 添加所有修改
git add .

# 提交更改
git commit -m "配置 Vercel 代理解决混合内容问题"

# 推送到 GitHub
git push origin main
```

### 步骤 3: 在 Vercel 手动触发重新部署

1. 进入 Vercel 项目页面
2. 点击 **"Deployments"** 标签
3. 等待自动部署完成，或者：
   - 点击 **"Redeploy"** 按钮
   - 选择 **"Use existing Build Cache"** 选择 **No**（清除缓存）
   - 点击 **"Redeploy"**

### 步骤 4: 验证部署

部署完成后（通常 1-3 分钟）：

1. 打开 https://ship-steel.vercel.app/
2. **强制刷新** (Ctrl+F5 或 Ctrl+Shift+R)
3. 按 **F12** 打开开发者工具
4. 查看 **Console** 标签

应该看到：
```
🔧 API 配置信息:
  - API_BASE_URL: /api/backend ✅
  - 当前环境: production
```

5. 查看 **Network** 标签
   - 查找对 `/api/backend/health` 的请求
   - 应该显示状态码 200 ✅

## 🎯 预期结果

### 配置成功后

- ✅ API_BASE_URL 显示为 `/api/backend`
- ✅ 请求路径为相对路径
- ✅ 不再有混合内容错误
- ✅ 后端服务连接成功

### 如果仍然有问题

1. **清除浏览器缓存**
   - 按 Ctrl+Shift+Delete
   - 选择"缓存的图像和文件"
   - 清除后刷新页面

2. **检查 Vercel 部署日志**
   - 进入 Vercel 项目
   - 点击部署记录
   - 查看构建日志是否有错误

3. **验证 vercel.json 是否在正确位置**
   - 文件应该在 `frontend/vercel.json`
   - 不是在项目根目录

## 📝 快速命令

```bash
# 在 frontend 目录执行
cd frontend
git add .
git commit -m "配置 Vercel 代理"
git push origin main
```

然后到 Vercel 控制台手动触发重新部署。

